package encapsulation;

public class Inheritance
{
    //Inheritance relationship is created by using the keywords "extends"
    //Type of inheritance :
                    //Single
                    // Multiple
                    // Multilevel = where super class (parent class) have one sub class (Child class )
                                    //and that sub class has another sub class
                    // Hybrid
                    // Hierarchical = where super class (parent class ) class have multiple sub class (child class )

    // Class --> class / interface --> interface --> extends

    // class --> interface --> implements


    //public class Animal{} as superclass
    //public class Dog extends Animal {} Dog is sub class class of Animal
    //public class Cat extends Dog {} Cat is subclass of Animals


    public static void main (String [] args)
    {

    }

}
